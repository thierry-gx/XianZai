package com.thierryguichardaz.xianzai.ui.theme // Assicurati che il package sia giusto

import androidx.compose.ui.graphics.Color

// --- Colori per Dark Theme XianZai ---
val DarkNavy = Color(0xFF0A192F)        // Sfondo molto scuro
val Navy = Color(0xFF112240)           // Sfondo per superfici/Card
val LightNavy = Color(0xFF233554)       // Variante più chiara per sfondi (es. opzione selezionata)

val TealAccent = Color(0xFF64FFDA)     // Accento primario (vibrante)
val TealAccentDark = Color(0xFF1DB99A)   // Variante scura dell'accento (per bordi selezionati?)

val LightSlate = Color(0xFFCCD6F6)      // Testo principale (quasi bianco)
val Slate = Color(0xFF8892B0)           // Testo secondario/Hint
val DarkSlate = Color(0xFF495670)       // Bordi / Elementi meno importanti

// --- Colori per Light Theme XianZai (NUOVI) ---
val LightBackground = Color(0xFFF8F9FA) // Sfondo chiaro (quasi bianco)
val LightSurface = Color.White           // Sfondo per superfici/Card (bianco puro)
val LightSurfaceVariant = Color(0xFFE9ECEF) // Variante più scura per sfondi chiari (es. TopAppBar)

val BluePrimary = Color(0xFF007BFF)      // Accento primario per tema chiaro (es. Blu)
val BluePrimaryDark = Color(0xFF0056b3)    // Variante scura dell'accento chiaro

val DarkText = Color(0xFF212529)         // Testo principale scuro
val MediumGrayText = Color(0xFF495057)     // Testo secondario/Hint scuro
val LightGrayBorder = Color(0xFFDEE2E6)   // Bordi / Elementi meno importanti chiari

// Colori di default Material (puoi rimuoverli se non usati altrove o tenerli per riferimento)
val Purple80 = Color(0xFFD0BCFF)
val PurpleGrey80 = Color(0xFFCCC2DC)
val Pink80 = Color(0xFFEFB8C8)

val Purple40 = Color(0xFF6650a4)
val PurpleGrey40 = Color(0xFF625b71)
val Pink40 = Color(0xFF7D5260)